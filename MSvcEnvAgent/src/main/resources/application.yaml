server:
  port: 8080
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${REGISTRY}/eureka

mvf4ms:
  svcenv:
    notifySvcName: MGroupAgent

spring:
  application:
    name: MSvcEnvAgent
